milvus:
  host: "${{MILVUS_HOST:-localhost}}"
  port: ${{MILVUS_PORT:-19530}}
collection:
  name: "earnings_calls_compression"

embeddings:
  model: "sentence-transformers/all-MiniLM-L6-v2"

compression:
  type: "reranking"
  top_k: 5
  reranker:
    type: "cross_encoder"
    model: "cross-encoder/ms-marco-MiniLM-L-12-v2"

retrieval:
  top_k: 20

rag:
  enabled: false

logging:
  name: "milvus_earnings_calls_reranking"
  level: "INFO"
