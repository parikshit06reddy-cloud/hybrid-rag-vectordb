# QDRANT Agentic RAG Pipeline - FactScore Dataset

qdrant:
  host: "${QDRANT_HOST:-localhost}"
  port: "${QDRANT_PORT:-6333}"
  api_key: "${QDRANT_API_KEY:-}"

collection:
  name: factscore_agentic_rag

dataloader:
  type: factscore
  dataset_name: dskar/FActScore
  split: test
  limit: null

generator:
  model: llama-3.3-70b-versatile
  api_key: "${GROQ_API_KEY:-}"
  max_tokens: 2048
  temperature: 0.7

embeddings:
  model: Qwen/Qwen3-Embedding-0.6B
  batch_size: 32

agentic_rag:
  model: llama-3.3-70b-versatile
  api_key: "${GROQ_API_KEY:-}"
  routing_enabled: true
  self_reflection_enabled: true
  max_iterations: 3
  quality_threshold: 80
  fallback_tool: retrieval
  max_retries: 3
  retry_delay_seconds: 0.5

evaluation:
  enabled: true
  metrics:
  - contextual_recall
  - contextual_precision
  - answer_relevancy
  - faithfulness

logging:
  name: qdrant_agentic_rag_factscore
  level: INFO

indexing:
  chunk_size: 512
  chunk_overlap: 50
  batch_size: 32

retrieval:
  top_k_default: 10
